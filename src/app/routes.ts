import { Route } from "@angular/router";
import { CodeAreaComponent } from "./app-code-area/code-area.component";

export const ROUTES: Route[] = [
    {
        path: '',
        component: CodeAreaComponent
    }
];